# 建置環境不需要的東西。少了這個，建置會把整個 .venv 送進建置上下文。
.venv/
venv/
__pycache__/
*.py[cod]

# 執行期資料與機密：容器靠 volume 掛載，不進映像檔
.git/
.gitignore
data/
logs/
backups/
assets/
config/persona.md
.env

# 開發用檔案：Dockerfile 不會 COPY 它們，排除只是為了加快建置
tests/
deploy/
*.md
!README.md
# scripts/ 不可以排除 —— Dockerfile 會 COPY 它，排除會讓建置失敗

# 兩個 Hermes Agent 嘅家（SOUL.md、config、session…）—— 同 Router 個 image 無關
# 唔排除嘅話會將成個 Hermes 資料目錄（~100MB）送入 build context，
# 而且 belfast/data/ 入面有 xAI key。
#
# ⚠️ 呢兩個唔可以刪。Dockerfile 只 COPY dafeijing/ 同 scripts/，
#    所以就算唔排除都唔會入到映像檔 —— 但 build context 會白送 100MB 上 daemon。
hermes/
belfast/
